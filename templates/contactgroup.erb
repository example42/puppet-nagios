# File Managed by Puppet
define contactgroup{
        contactgroup_name               <%= @name %>
        alias                           <%= @alias %>
        members                         <%= @members %>
        }
